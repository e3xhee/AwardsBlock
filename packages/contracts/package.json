{
  "name": "@awardblock/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "forge build && node scripts/copyAbi.mjs",
    "copy:abi": "node scripts/copyAbi.mjs",
    "test": "forge test",
    "lint": "forge fmt --check",
    "format": "forge fmt"
  },
  "devDependencies": {
    "@foundry-rs/forge-win32-amd64": "1.7.1",
    "@openzeppelin/contracts": "^5.6.1"
  }
}
