{
  "name": "@awardblock/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "test": "tsc --noEmit && node scripts/run-tests.mjs",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@awardblock/shared": "workspace:*",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@vitejs/plugin-legacy": "^5.4.0",
    "tsx": "^4.19.0",
    "vite": "^5.4.0"
  }
}
